agent: jc1
ptx: |
  .version 8.6
  .target sm_87
  .address_size 64

  .visible .entry kernel(
    .param .u64 ptr
  ) {
    .reg .u64 %r0;
    ld.param.u64 %r0, [ptr];
    mov.u32 %r1, 42;
    st.global.u32 [%r0], %r1;
    ret;
  }
